<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Màu nền của View -->
    <corners android:radius="20dp" /> <!-- Độ bo tròn góc (ở đây là 20dp) -->
</shape>
